[package]
name = "forgedash-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
glow = "0.14"
image = { version = "0.25", default-features = false, features = ["png"] }
fontdue = "0.9"
